{
  "name": "lifeline-phc-triage",
  "version": "1.0.0",
  "description": "LifeLine: Pre-consultation rural PHC triage with deterministic red-flag safety rails and constrained AI prioritization",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client:dev": "npm --prefix client run dev",
    "test": "node --test server/tests/*.test.js",
    "build": "npm --prefix client run build"
  }
}
